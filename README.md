# UserLogin-Postgresql
This is a personal project done to create authorize and authenticate user login using javascript(node.js and express.js included), apis, typescript and postgresql as the database
